# Zerion Core (required) — terminal front end, agents, skills, tools,
# 24/7 runtime. All pure-Python, wheel-safe on desktop linux and Termux/ARM64.
requests>=2.31
python-dotenv>=1.0

# --- Web UI -------------------------------------------------------------
# For the full product experience (browser UI, /ws bridge, REST panels):
#   pip install -r ui/requirements-ui.txt
# Starlette + vanilla uvicorn — pure Python, Termux/ARM64-safe, zero compilers.


# Voice output uses Gemini's TTS API via `requests` (already required above)
# plus Python's stdlib `wave` module — no extra Python packages. Playback
# needs one CLI audio player on PATH:
#   Termux (Android): pkg install termux-api   (provides termux-media-player)
#   Linux/desktop:    mpv, ffplay, aplay, or paplay

# psutil (optional but recommended) — richer CPU/RAM/process/battery data
# for tools and the UI System Status panel. Without it those fall back to
# /proc parsing (Linux/Termux) or report "unavailable" instead of failing.
# NOTE: psutil compiles from source on Termux — needs: pkg install clang make
# psutil>=5.9

# pytest — only needed to run the test suite (`python -m pytest tests/ -q`);
# the test files are stdlib-unittest based but the harness expects pytest.
# pytest>=8
