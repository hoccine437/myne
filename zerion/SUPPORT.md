# Support

Start with `QUICK_RUN.md`, `REQUIREMENTS.md`, and runtime reports in the repository root. For issues, include Python version, operating system/Termux version, sanitized `config.validate()` output, exact command, and traceback. Never include `.env`, provider keys, private memory, contacts, or phone data.
